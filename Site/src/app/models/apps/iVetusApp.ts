export interface IVetusApp{
    /**
     * Public name that's displayed to the user
     */
    name: string;
    
    /**
     * Used to trigger the app in scripts
     */
    triggerName: string;   
}